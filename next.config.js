/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.pravatar.cc', 'img.icons8.com', 'images.unsplash.com', 'flagcdn.com', 'www.svgrepo.com'],
  },
};

module.exports = nextConfig;